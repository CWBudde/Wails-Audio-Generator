# README

## About

This is a simple audio frequency generator written in Go and React-Typescript. It uses the Wails framework to
provide a Go backend and a React frontend. The frontend is built using Vite.
